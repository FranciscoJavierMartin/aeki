meta {
  name: Remove budget 2
  type: http
  seq: 4
}

delete {
  url: {{baseUrl}}/budgets/{{budgetId}}/{{productIdToDelete}}
  body: json
  auth: none
}

body:json {
  {
    "productId": "{{productIdToDelete}}"
  }
}
